; pnpm configuration
; React Native requires certain packages to be hoisted to the root node_modules
; to avoid "Unable to resolve module" errors during Metro bundling.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false