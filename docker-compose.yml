services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TURSO_DATABASE_URL=file:local.db
    volumes:
      - ./data:/app/data