{
  "name": "@droidvibe/native-usb",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo no tests"
  },
  "dependencies": {
    "@droidvibe/shared": "workspace:*"
  },
  "devDependencies": {
    "expo-modules-core": "^1.12.0",
    "typescript": "^5.6.3"
  }
}